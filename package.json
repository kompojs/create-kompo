{
  "name": "create-kompo-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Create Kompo App - Monorepo for create-kompo packages",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "version": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "tsup": "^8.5.1",
    "turbo": "^2.8.14",
    "typescript": "^5.9.3"
  }
}
